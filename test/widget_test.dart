import 'package:flutter_test/flutter_test.dart';


void main() {
  testWidgets('App builds successfully', (WidgetTester tester) async {
    // Test is intentionally minimal for now
    expect(true, isTrue);
  });
}
